# Client-side environment variables (these will be exposed to the browser)
# Only add VITE_ prefixed variables here - never add secrets!

# Supabase public configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_public_key

# API configuration
VITE_API_ROOT=/api/v1/

# NOTE: NEVER add secret keys like SUPABASE_SERVICE_ROLE_KEY here
# Secret keys should only be in server-side environment files